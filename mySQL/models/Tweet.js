import { Sequelize } from "sequelize";

module.exports = sequelize.define("Tweets", { // will take the name of model we wanted to create Tweet is basically a table(document)

//all the schema attribute inside their is going to represent  column to this table tweet



});